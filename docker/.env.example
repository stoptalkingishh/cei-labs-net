# cei-labs-net :: per-challenge environment template
# Copy to .env alongside docker-compose.yml and fill in per challenge.
# .env is gitignored — never commit real challenge secrets/flags here.

CHALLENGE_NAME=example-challenge
CHALLENGE_IMAGE=registry.example.internal/cei-labs/example-challenge:latest
CHALLENGE_PORT=9001
